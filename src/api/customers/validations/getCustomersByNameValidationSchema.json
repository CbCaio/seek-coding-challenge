{
  "params": {
    "type": "object",
    "required": [
      "name"
    ],
    "properties": {
      "name": {
        "type": "string",
        "enum": ["unilever", "nike", "apple", "ford","default"]
      }
    },
    "additionalProperties": false
  }
}
