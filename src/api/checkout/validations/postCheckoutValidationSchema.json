{
  "payload": {
    "type": "object",
    "required": [
      "customer",
      "product"
    ],
    "properties": {
      "customer": {
        "type": "string",
        "enum": ["unilever", "nike", "apple","ford", "default"]
      },
      "product": {
        "type": "string",
        "enum": ["classic", "standout", "premium"]
      }
    },
    "additionalProperties": false
  }
}
